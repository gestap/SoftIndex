// hooks
import { makeStyles } from '@material-ui/core/styles';

export const styles = makeStyles({
  select: {
    minWidth: 300,
  },
  box: {
    marginTop: 40,
    justifyContent: 'flex-end',
    display: 'flex',
  },
  button: {
    marginLeft: 20,
  },
});
